 - add logging
 - fix script filter protocol (length of message in first line)
 - extract bulk size to config

to be decided:
 - should stop if publish fails for at least one message
 - binary message support
